---
description: GitHub Issue를 컨벤션(docs/issue-convention.md)에 맞춰 scripts/new-issue.sh로 생성
alwaysApply: false
---

# 이슈 생성 규칙

사용자가 "이슈 만들어줘" 류의 요청을 하면:

- **컨벤션 정본은 `docs/issue-convention.md`** — 제목·ID·본문·라벨 규칙을 따른다.
- **직접 `gh issue create`를 손으로 만들지 말고 `scripts/new-issue.sh`를 사용**한다.
- 요청에서 유형(feat/setup/prototype/infra/deploy/fix/chore)·ID·제목(명사형)·순위·페이지·참고(docs/proposal/11~14)·완료조건·체크리스트를 뽑아 인자로 넘긴다.
  - 카탈로그(`docs/proposal/14`, `docs/issues-list.md`)에 있으면 그 내용을 재사용한다.
- 먼저 `--dry-run`으로 조립 결과를 보여주고, 확인되면 실제 등록한다.
- 등록 후 이슈 번호·제목·라벨을 보고한다.
- gh CLI 인증(`gh auth login`)이 선행 필요.

예: `scripts/new-issue.sh --type feat --id F-06 --priority 1 --page "분석 /analysis/[id]" --title "산업 파급 경로 연결지도 시각화" --done "…" --ui "…" --api "…" --demo "…" --dry-run`
